export type tenant = {
    id: string,
    github_username: string,
    is_public: boolean,
    name: string,
    permission: string,
    preferred_compression_method: string,
    uri: string,
    priority: number
}
