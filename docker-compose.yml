services:
    postgres:
        image: postgres:18.3
        ports:
            - 5432:5432
        environment:
            - POSTGRES_PASSWORD=postgres
            - POSTGRES_DB=iglu
        command: ["-c", "log_error_verbosity=verbose"]
