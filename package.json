{
    "name": "iglu",
    "type": "module",
    "private": true,
    "devDependencies": {
        "@types/bun": "latest"
    },
    "peerDependencies": {
        "typescript": "^5"
    },
    "dependencies": {
        "@biomejs/biome": "^2.4.10"
    },

    "scripts": {
        "format": "bunx biome format --write",
        "lint": "bunx biome lint",
        "cache::dev": "bun run --hot cache/index.ts"
    }
}
